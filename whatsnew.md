## 🧩 Комміти

### 🟢 main
> Головний коміт, з якого все почалось.

---

### 🆕 Оновлення: **voting & users**
- 🔁 Оновились модулі **голосувань** і **користувачів**  
- 🗑️ Старі дані видалено  
- ⚠️ Додано **попереджувальне повідомлення**:  
  > «Ви вже голосували в цьому опитуванні»

---

### 🆕 Оновлення: **results & validity for completed polls**
- 📅 Завершені опитування тепер **відкриваються за адресою** `/polls/<id>` (без 404 після `end_at`)
- 📊 Додано **підрахунок результатів** (загальна к-сть, к-сть по кожній опції, %)
- 🏷️ Додається **бейдж дійсності**: «Дійсне» / «Недійсне» (за кворумом)
- 🖼️ Шаблон detail: **форма ховається** після завершення, показується **таблиця результатів**
- 🧩 Технічні зміни:
  - `views.py`: `PollDetailView.get_queryset()` тепер пропускає **ACTIVE + COMPLETED** (і `end_at < now`)
  - `views.py`: у `get_context_data()` додаються `is_finished`, `totals`, `is_valid`, `validity_note`
  - `services.py` (новий): `tally_poll(poll)`, `check_validity(poll, total_votes)`
  - `detail.html`: додано таблицю з к-стю/%, бейдж дійсності

#### 🔨 Міграції
- (необов’язково) Якщо додано поле `expected_turnout` для кворуму:  
  `python manage.py makemigrations polls && python manage.py migrate`

#### ✅ Як перевірити
1) В адмінці постав `status = completed` або `end_at` у минуле.  
2) Переконайся, що в MongoDB є голоси для `poll_id`.  
3) Відкрий `/polls/<id>` — бачиш таблицю та бейдж дійсності.

---

## 🆕 Оновлення: **adminpanel**
- 📂 **`/adminpanel/`** — список **моїх** опитувань (фільтр `admin = request.user`), картки з назвою, датами та статусом.  
- ➕ **`/adminpanel/create/`** — створення **Poll** + **PollOption** (мінімум 2 варіанти).  
  Поля: `title`, `short_description`, `start_at`, `end_at`, `can_change_vote`, `quorum`, `expected_turnout`, `status`.  
- 🔎 **`/adminpanel/<id>/`** — перегляд як у користувача, але з додатковою службовою інформацією:
  - 📊 загальна кількість голосів (MongoDB);
  - 🧮 кількість голосів по кожній опції;
  - 🏷️ дійсність за кворумом (`total_votes >= ceil(expected_turnout * quorum / 100)`; якщо `expected_turnout` не заданий — Н/Д);
  - ⏱️ кнопка **«Завершити зараз»**: встановлює `end_at = now` і `status = completed`.
- 🧩 **Технічні зміни:**
  - `adminpanel/urls.py` — маршрути: `list`, `create`, `detail`, `finish`;
  - `adminpanel/views.py` — `MyPollListView`, `PollCreateView`, `PollAdminDetailView`, `finish_now`;
  - `adminpanel/forms.py` — `AdminPollForm` + `OptionFormSet (min_num=2)`;
  - `templates/adminpanel/` — `list.html`, `create.html`, `detail.html`;
  - `polls/templatetags/polls_extras.py` — фільтр `get_item` (для доступу до словника в шаблоні);
  - читання з Mongo через `polls.mongo.get_votes_collection()`.
- 🧱 **Міграції:** не потрібні (моделі не змінювалися).

### ✅ Як перевірити
1. Відкрити `/adminpanel/` — відображаються лише опитування поточного адміністратора.  
2. Створити нове опитування на `/adminpanel/create/` (мінімум 2 варіанти).  
3. Проголосувати кількома користувачами.  
4. Відкрити `/adminpanel/<id>/` — перевірити:
   - загальну кількість голосів;
   - кількість голосів по опціях;
   - статус дійсності (кворум);
   - кнопку **«Завершити зараз»**.  
5. Натиснути **«Завершити зараз»** — має оновитися `status = completed` і `end_at ≈ now`.  
6. Перевірити в MongoDB:
   ```js
   db.votes.countDocuments({ poll_id: <id> })
   db.votes.aggregate([
     { $match: { poll_id: <id> } },
     { $group: { _id: "$option_id", votes: { $sum: 1 } } }
   ])

---

## 🆕 Оновлення: **Access & Timing & Templates**

### 🔐 Доступ до адмінпанелі
- Сторінки `/adminpanel/*` тепер доступні **лише** користувачам у групі `admin`.
- Додано міксин `AdminGroupRequiredMixin` (усередині вже є перевірка логіну + перевірка групи).
- Неавторизованим або не-адмінам повертається **403 Forbidden** або редірект на сторінку входу для адмінів (залежно від конфігурації).
- `accounts`: сторінки входу розділені:
  - `login_user` для звичайних користувачів;
  - `login_admin` для адміністраторів.

### ⏱️ Вікна голосування
- Сторінка опитування відкривається в усіх станах, але:
  - **До початку** — форма **не показується**, замість неї інформаційна плашка *«ще не почалося»*.
  - **Під час** — показується форма голосування.
  - **Після завершення** — **лише результати** (таблиця підсумків + бейдж дійсності).
- Логіка реалізована у `PollDetailView.get_context_data()` через прапорці `before_start` та `is_finished` (метод перероблено).

### ✅ Валідації
- Перевірка `start_at < end_at` (у моделі/формі адміністратора).
- Обов’язкові поля — як раніше; **email-валідатор пропущено** (у формі голосування поля немає).

### 🖼️ Шаблони і час
- `polls/detail.html`: під заголовком показуються **Початок / Кінець** у форматі `d.m.Y H:i`; у блоці “ще не почалося” дублюються обидві дати.
- `polls/list.html`: у списку опитувань під назвою відображаються **Початок / Кінець**.
- Використано `|localtime|date:"d.m.Y H:i"` для коректного локального відображення (за умови `USE_TZ = True`).

### ⚙️ Налаштування
- Додано або уточнено у `settings.py`:
  ```python
  LOGIN_URL = "login_user"
  LOGIN_REDIRECT_URL = "/polls/"

### ✅ Як перевірити

#### 🔐 Адмінпанель
- Анонім або звичайний користувач → `/adminpanel/` → **403** або редірект на `/accounts/login-admin/`.
- Адмін → має повний доступ.

#### ⏱️ Опитування
- `start_at > now` → видно плашку **«ще не почалося»**, форми немає.
- `start_at < now < end_at` → видно **форму**, голос приймається.
- `end_at < now` або `status = completed` → видно **результати** (без форми).

#### 🖼️ Час
- У шаблонах `detail` і `list` видно **Початок / Кінець** у локальному форматі.
- На межі (`end_at = now`) форма зникає після завершення, і відображаються лише результати.

---

### 🆕 Оновлення: ‘Already voted’ banner — тільки якщо зміна заборонена

#### Суть змін
Плашка «Ви вже голосували» тепер з’являється лише тоді, коли користувач уже голосував і не має права змінювати голос. Якщо зміна дозволена (`can_change_vote=True` або `poll.can_change_vote_for(user)=True`), повідомлення не показується.

#### Зміни у `views.py`

У `PollDetailView.get_context_data(...)` додано три змінні:
- **user_has_voted** — перевіряє, чи користувач уже голосував у цьому опитуванні;
- **can_change_for_user** — визначає, чи дозволено користувачу змінювати голос;
- **show_already_voted_banner** — спрацьовує лише коли `user_has_voted` і при цьому `not can_change_for_user`.

Логіка розрахунку `totals` і показу форми голосування (`VoteForm`) залишилася без змін.

У `PollDetailView.post(...)` змінено поведінку при повторному голосуванні:
- Якщо зміна дозволена — голос оновлюється і користувача перенаправляє на сторінку підтвердження.
- Якщо зміна заборонена — з’являється повідомлення про помилку, і сторінка перерендерюється з контекстом, у якому відображається плашка.

Також додано хелпер `_can_change_for_user(poll, user)`, який викликає метод `poll.can_change_vote_for(user)` (якщо він існує) або використовує булеве поле `poll.can_change_vote`.

#### Зміни у `templates/polls/detail.html`

Видалено старий банер із тегом `{% has_voted %}`.  
Новий банер вставлено лише в активній фазі голосування:

Виводиться, коли `show_already_voted_banner` має значення `True`.

#### Перевірка вручну

| Стан опитування | Сценарій користувача | Очікування |
|------------------|----------------------|-------------|
| Активне, `can_change_vote=False` | Уже голосував | Плашка показується |
| Активне, `can_change_vote=True` | Уже голосував | Плашка не показується |
| Ще не почалося | Будь-який | Плашка відсутня |
| Завершене | Будь-який | Плашка відсутня |
| Індивідуальний дозвіл (`can_change_vote_for(user)=True`) | Уже голосував | Плашка відсутня |

---

### 🆕 Оновлення: **реєстрація, вихід та обмеження пароля**

- 📝 **Позначено необов’язкові поля**  
  На сторінках реєстрації користувача й адміністратора додано підписи біля полів, які **не є обов’язковими** для заповнення.  
  У шаблонах тепер відображається текстове пояснення *(необов’язкове поле)* поруч із відповідними елементами форми.

- 🔘 **Кнопка “Вийти” (Logout)**  
  Додано кнопку **«Вийти»** для безпечного виходу із системи:
  - на сторінці **списку голосувань користувача** (`polls/list.html`);
  - на сторінці **панелі адміністратора** (`adminpanel/list.html`).  
  Після натискання виконується **вихід із системи** та **перенаправлення на головну сторінку сайту**.  
  Використано `django.contrib.auth.views.LogoutView` із POST-формою на фронтенді та GET-виходом в адмінці.

- ⚙️ **Обмеження символів у паролі адміністратора**  
  У формі реєстрації адміністратора додано інформаційне поле з попередженням:  
  > Не можна вводити такі символи: `[!@#$%^&*(),.?":{}|<>]`  

  Повідомлення відображається безпосередньо на сторінці реєстрації адміністратора.

---

### 🆕 Оновлення: **пароль адміністратора — підтримка спецсимволів**

- 🔓 **Знято обмеження** на використання спеціальних символів у паролі адміністратора.  
  Тепер дозволяються символи: `[!@#$%^&*(),.?":{}|<>]`.

- 🧩 **Шаблон `register_admin.html`**  
  Видалено текстове попередження про заборонені символи.  
  Форма реєстрації тепер приймає будь-які стандартні символи, підтримувані Django PasswordValidator.

---
  
### 🆕 Оновлення: **UI Redesign**

- 🎨 Додано **оновлений сучасний дизайн сторінки**
- 🧩 Оновлено стилі: структура, кольори, відступи, компоненти інтерфейсу
- 🖼️ Сторінка стала **чистішою, візуально гармонійною та зручнішою для користувача**
- 📱 Поліпшено адаптивність та взаємодію елементів на різних екранах

---

### 🆕 Оновлення: **anonymous voting — повна підтримка**

- 🕶️ Додано можливість створювати **анонімні голосування** через нове поле `is_anonymous` у формі адміністратора.
- 🔐 Реальні `user_id` більше **не зберігаються** в MongoDB для анонімних опитувань.  
  Натомість генерується криптографічний хеш формату:  
  `anon:<sha256(poll_id + base_id + ANON_VOTE_SALT)>`  
  який:
  - не дозволяє встановити особу користувача;
  - гарантує захист від повторного голосування;
  - працює для авторизованих і неавторизованих (через `session_key`).

- 🚫 Логіка «вже голосував» тепер працює і для анонімних опитувань —  
  повторне голосування блокується або оновлюється відповідно до `can_change_vote`.

- 🙈 **Адміністратор не бачить результати по варіантах**, доки опитування не завершене.  
  У `adminpanel/detail.html` показується повідомлення:  
  > «Результати по варіантах будуть доступні після завершення.»

- 📅 Після завершення (`end_at <= now` або статус `completed`) результати відкриваються повністю — як для адміна, так і для користувача.

- 🧩 Технічні зміни:
  - модифіковано `polls/mongo.py`: новий метод `get_user_id_for_poll`;
  - `polls/views.py`: оновлена логіка вставки/перевірки голосів;
  - `adminpanel/views.py`: приховування результатів у незавершених анонімних голосуваннях;
  - `adminpanel/detail.html`: умовний рендер блоку “Варіанти”;
  - форма створення (`adminpanel/create.html`) тепер включає `is_anonymous`.

- 🧱 Міграції:  
  додано поле `is_anonymous` →  
  `python manage.py makemigrations && python manage.py migrate`

📅 *Дата коміту:* `2025-11-30`  
💬 *Автор:* Arsen Muska