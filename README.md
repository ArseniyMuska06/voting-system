# 🗳️ Voting System  
### Deployment & Usage Guide

---

## 📌 Опис проєкту

Цей репозиторій містить **веб-застосунок системи голосування**, розроблений з використанням фреймворку **Django**.  
Система підтримує:
- розділення ролей користувачів (користувач / адміністратор);
- створення та проходження опитувань;
- зберігання голосів у **нереляційній базі даних MongoDB** з метою підвищення рівня анонімності.

---

## ⚙️ Вимоги до середовища

Для розгортання та коректної роботи веб-застосунку необхідно мати:
- сучасний веб-браузер;
- **Python 3.10 або вище**;
- **Django**;
- **MongoDB** (локальна або віддалена);
- менеджер пакетів `pip`.

Усі Python-залежності описані у файлі `requirements.txt`.

---

## 🚀 Налаштування та розгортання

### 1️⃣ Активація віртуального середовища

Після клонування репозиторію необхідно активувати віртуальне середовище:

```bash
.venv/scripts/activate
```

### 2️⃣ Встановлення залежностей

Далі слід перейти до каталогу репозиторію та встановити всі необхідні залежності:

```bash
cd voting_system
pip install -r requirements.txt
```

### 3️⃣ Виконання міграцій Django

Після встановлення залежностей необхідно виконати міграції фреймворку Django:

```bash
python manage.py makemigrations
python manage.py migrate
```

У результаті буде автоматично створено таблиці реляційної бази даних для зберігання інформації про користувачів, опитування та варіанти відповідей відповідно до описаних моделей.

---

## 🗄️ Робота з MongoDB

Для зберігання голосів використовується **MongoDB**.  
Такий підхід дозволяє відокремити інформацію про голосування від персональних даних користувачів та підвищити рівень анонімності системи.

Після встановлення MongoDB веб-застосунок автоматично підключається до бази даних та створює окрему колекцію для зберігання голосів користувачів.

---

## ▶️ Запуск застосунку

Після завершення налаштувань баз даних веб-застосунок запускається командою:

```bash
python manage.py runserver
```

Після запуску система стає доступною через браузер за локальною адресою, що відображається у консолі.

---

## 🛠️ Адміністрування системи

Для керування системою необхідно мати обліковий запис адміністратора.  
Створення адміністратора виконується командою:

```bash
python manage.py createsuperuser
```

Після виконання команди необхідно ввести ім’я користувача, адресу електронної пошти та пароль.  
Після авторизації адміністратор отримує доступ до адміністративної панелі керування опитуваннями.
